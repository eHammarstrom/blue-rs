## Requirements

* `probe-run` + `defmt` feature
* `flip-link`

Like so,
```
cargo install flip-link
cargo install \
    --git https://github.com/knurling-rs/probe-run \
    --branch main \
    --features defmt
```
